{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "Lightweight API Gateway routing requests to Auth, Product, and Payment microservices",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "lint": "eslint ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "eslint": "^9.0.0",
    "express-http-proxy": "^2.0.0"
  }
}
